import React from "react";
import {
    BrowserRouter as Router,
    useHistory,
    useParams
  } from "react-router-dom";

  const Hero = () => {
  return (
    <div className="hero-container">
      
    </div>
  );

};

export default Hero;